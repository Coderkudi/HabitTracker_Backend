services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: habittracker
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  pgdata: {}
